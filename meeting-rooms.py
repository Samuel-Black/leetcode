from typing import List
# import heapq

class Solution:
  def canAttendMeetings(self, intervals: List[List[int]]) -> bool:
    
    canAttend = True
    
    upperBound = intervals[0][0]
    lowerBound = intervals[0][1]
    
    intervals.pop(0)
    
    for interval in intervals:
      if 
      
    return canAttend